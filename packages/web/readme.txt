live agora
--

install modules:

$ npm install

start local development server:

$ netlify dev

--

Environment variables:

$ netlify env:set VITE_APP_URL https://preview.taat.live
$ netlify env:set VITE_HOCUSPOCUS_URL wss://hocuspocus.taat.live