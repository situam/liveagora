/**
 * Space ydoc field names
 * TODO: populate with rest
 */
export const SpaceDocFields = {
  nodeSidebar(nodeId: string) {
    return `post-for-node.${nodeId}`
  },
  sidebar: 'pad.space-sidebar',
}